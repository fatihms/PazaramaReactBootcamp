import { createSlice } from "@reduxjs/toolkit";

const codeSlice = createSlice({
  name: "code",
  initialState: {
    value: "4h2y00tcux",
  },
  reducers: {},
});

export default codeSlice.reducer;
